Portable PHP for Xander School Desktop.

Run from desktop-app:

    npm run fetch-php

That downloads PHP (NTS x64) into this folder so the packaged .exe does not need XAMPP.

Dev fallback: C:\xampp7\php\php.exe
